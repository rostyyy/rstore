# Імпортуємо функцію path для створення URL-маршрутів
from django.urls import path

# Імпортуємо модуль views, де знаходиться логіка обробки запитів
from . import views


# Список маршрутів додатку відгуків
urlpatterns = [

    # Додавання відгуку до товару
    # <int:product_id> - це динамічний параметр, який передає ID товару
    # Наприклад: /reviews/add/5/ додасть відгук до товару з ID=5
    # Форма відгуку відправляє POST-запит на цю адресу
    # Після додавання відгуку користувач перенаправляється на сторінку товару
    # Відгуки за замовчуванням не схвалені (is_approved=False) і потребують модерації
    path('add/<int:product_id>/', views.add_review, name='add_review'),
]